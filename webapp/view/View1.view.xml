<mvc:View controllerName="project1.controller.View1"
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:core="sap.ui.core">
    <Page id="page" title="{i18n>title}">
        <content>
            <VBox class="sapUiSmallMergin">

                <Toolbar >
                    <ToolbarSpacer />
                    <SearchField id="searchField"
                        width="50%"
                        placeholder="Search by title..."
                        search=".onSearch"/>
                </Toolbar>
                <Table id="bookTable" items="{bookModel1>/books}" mode="MultiSelect">

                    <headerToolbar>
                        <Toolbar id="bookTableToolbar">
                            <Title id="bookTableTitle" text="Books" />

                            <ToolbarSpacer />

                            <Button text="Add Record" press=".onAddRecord" type="Accept"/>
                            <Button text="Delete Record" press=".onDeleteRecord" type="Reject"></Button>

                            <ToolbarSeparator />

                            <Select id="genreSelect" change=".onGenreChange" width="150px" items="{
                                path: 'genreModel>/genres',
                                templateShareable: true}">
                                <core:Item key="{genreModel>key}" text="{genreModel>text}" />
                            </Select>

                            <Button text="Filter" press=".onApplyFilter" type="Emphasized"/>
                        </Toolbar>
                    </headerToolbar>

                    <columns>
                        <Column ><Text text="ID"/></Column>
                        <Column ><Text text="Name"/></Column>
                        <Column ><Text text="Author"/></Column>
                        <Column ><Text text="Genre"/></Column>
                        <Column ><Text text="Release Data"/></Column>
                        <Column ><Text text="Available Quantity"/></Column>
                        <Column ><Text text="Actions"></Text></Column>
                    </columns>

                    <items>
                        <ColumnListItem>
                            <cells>
                                <Text text="{bookModel1>ID}"></Text>
                                <HBox>
                                    <Text text="{bookModel1>Name}" visible="{= !${bookModel1>editMode} }"/>
                                    <Input value="{bookModel1>Name}" visible="{bookModel1>editMode}"/>
                                </HBox>
                                <Text text="{bookModel1>Author}"></Text>
                                <Text text="{bookModel1>Genre}"></Text>
                                <Text text="{bookModel1>ReleaseDate}"></Text>
                                <Text text="{bookModel1>AvailableQuantity}"></Text>
                                <HBox >
                                    <Button text="Edit" 
                                        visible="{= !${bookModel1>editMode}}"
                                        press=".onEditTitle" />

                                    <Button text="Save"
                                        type="Accept" 
                                        visible="{bookModel1>editMode}"
                                        press=".onSaveTitle"/>
                                </HBox>
                            </cells>
                        </ColumnListItem>
                    </items>
                </Table>

            </VBox>
        </content>
    </Page>
</mvc:View>