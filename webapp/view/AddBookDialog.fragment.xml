<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core">

    <Dialog id="addBookDialog"
        title="Add New Book"
        contentWidth="400px">

        <VBox renderType="Bare" class="sapUi-Std-PaddingS">
            <Input id="newName" placeholder="Name"  required="true" liveChange=".onLiveValidate" />
            <Input id="newAuthor" placeholder="Author" required="true" liveChange=".onLiveValidate" />
            <ComboBox id="newGenre" items="{
                path: 'genreModel>/genres'}" placeholder="Genre" required="true" change=".onLiveValidate" >
                <core:Item key="{genreModel>key}" text="{genreModel>text}"  />
            </ComboBox>
            <DatePicker id="newDate" placeholder="YYYY-MM-DD" displayFormat="yyyy-MM-dd"
                 valueFormat="yyyy-MM-dd" required="true" change=".onLiveValidate"/>
            <Input id="newQuantity" placeholder="Available Quantity" type="Number" required="true" liveChange=".onLiveValidate"/>
        </VBox> 

        <beginButton>
            <Button text="Add" press=".handleAddButton"/>
        </beginButton>

        <endButton>
            <Button text="Cancel" press=".handleCancelButton"/>
        </endButton>

    </Dialog>

</core:FragmentDefinition>